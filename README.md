## bleu
Link: (https://www.chrissuu.com/bleu)

Link (github pages, old): (https://chrissuu.github.io/bleu)

## Run locally (Docker)
Requires Docker + Docker Compose.

```bash
docker compose up --build
```

Then open `http://localhost:4000`. File edits (e.g. in `_posts/`) rebuild automatically; LiveReload is enabled.
If your browser doesn’t auto-refresh, install a LiveReload extension and point it at port `35729`.

## Credits
Theme credits to user rhl-bthr (https://github.com/rhl-bthr)

## license
[GNU GPL v3](LICENSE)
